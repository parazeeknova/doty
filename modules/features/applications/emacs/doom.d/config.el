;;; config.el -*- lexical-binding: t; -*-

;; Set user details
(setq user-full-name "przknv.cc"
      user-mail-address "yesh8harsh+github@gmail.com")

;; Set doom theme
(setq doom-theme 'doom-one)

;; Relative line numbers
(setq display-line-numbers-type 'relative)

;; Set font
(setq doom-font (font-spec :family "FiraCode Nerd Font" :size 12 :weight 'semi-bold)
      doom-variable-pitch-font (font-spec :family "FiraCode Nerd Font" :size 12))
